import { Box } from "@/components/ui/box";
import { Text } from "@/components/ui/text";

export default function ProfileScreen() {
  return (
    <Box style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
      <Text>Profile Page (待实现UI)</Text>
    </Box>
  );
}
